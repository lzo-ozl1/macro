name: Build EXE
on: [push]
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - run: pip install pyinstaller pynput
      - run: pyinstaller --onefile --windowed "쉬프트 매크로 윈도우용.py"
      - uses: actions/upload-artifact@v3
        with:
          name: macro-exe
          path: dist/
